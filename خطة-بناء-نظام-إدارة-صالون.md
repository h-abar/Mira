# خطة بناء نظام إدارة صالون حلاقة نسائي (Web Application)

## 1. نظرة عامة

نظام ويب متكامل لإدارة صالون حلاقة نسائي يشمل: المواعيد، الحسابات والمحاسبة، العملاء، الموظفات، الخدمات، والمخزون — بواجهة ثنائية اللغة (عربية/إنجليزية).

---

## نطاق التكافؤ مع برنامج زينة بلس (Zeina Plus Parity)

مقارنة حالة النظام الحالية مع ميزات برنامج **زينة بلس**:

| الميزة | الحالة في النظام الحالي |
|--------|--------------------------|
| المحاسبة (فواتير/مصروفات) | ✅ منفّذ |
| نقطة بيع خدمات/منتجات | ✅ منفّذ |
| نقطة بيع كافتيريا | ✅ منفّذ (تبويب في POS) |
| المستودعات | ✅ منفّذ |
| الأرباح والخسائر | ✅ منفّذ |
| المشتريات والموردين | ✅ منفّذ |
| المصروفات | ✅ منفّذ |
| العملاء | ✅ منفّذ |
| الموظفون | ✅ منفّذ |
| نقاط الولاء | ✅ منفّذ |
| الحجوزات | ✅ منفّذ |
| الخصومات والعروض | ✅ منفّذ |
| صلاحيات المستخدم | ✅ منفّذ (تفصيلية على مستوى كل وحدة/مسار — بند "فرض الأذونات على الجميع") |
| الإشعارات | ⚠️ جزئي (إشعارات WhatsApp عبر Cloud API، يتطلب تفعيل التوكن) |
| عربي/إنجليزي | ✅ منفّذ |
| الحضور والانصراف | ✅ منفّذ |
| الورديات (فتح/إغلاق + مطابقة النقدية) | ✅ منفّذ |
| الفوترة الإلكترونية ZATCA | ⚠️ جزئي (QR + CSR + XML sandbox — شهادة إنتاج رسمية مستقبلية) |
| العضويات/الباقات | ✅ منفّذ |
| بطاقات الهدايا | ✅ منفّذ |
| الإكراميات | ✅ منفّذ |
| النشر على Railway (إنتاج) | ✅ https://mira-production-296a.up.railway.app |

> ✅ منفّذ / ⚠️ جزئي / ❌ مستقبلي — تُحدَّث هذه الجدولة مع كل مرحلة من خارطة الطريق.

---

## تحليل المنافسين (Competitor Analysis)

### البرامج العربية

| البرنامج | البلد/السوق | أبرز الميزات | الفجوات في نظامنا |
|----------|-------------|--------------|-------------------|
| **زينة بلس** | الخليج | محاسبة، POS خدمات/منتجات/كافتيريا، مستودعات، P&L، مشتريات، ولاء، حجوزات، خصومات، صلاحيات، واتساب | كافتيريا POS، ZATCA |
| **نعيم (المشاغل)** | السعودية | حجوزات، **فروع متعددة**، تقارير دورية للموظفات، **تواصل ذكي (رسائل/واتساب مع روابط حجز)**، حجز أونلاين، مخزون، **مدفوعات إلكترونية (فيزا/مدى/ماستركارد/آبل باي)**، تذكير بالحجوزات، **معتمد ZATCA** | تذكير تلقائي، دفع إلكتروني، فروع |
| **رتم** | السعودية | **تقويم هجري**، امتثال محلي، جدولة الموظفات، عروض تسويقية مخصصة، أمان/أذونات | تقويم هجري، عروض مخصصة |
| **DEXEF** | مصر/الخليج | حجوزات، **سجل زيارات وخدمات مفضلة للعميلة**، مخزون، ولاء + **إشعارات تلقائية**، **تحليلات ذكية لعروض مخصصة** | تحليلات/عروض مخصصة |
| **صالوناتي** | الخليج | حجز أونلاين، فوترة، CRM، موظفين، مخزون، تقارير | — |

### البرامج العالمية

| البرنامج | الموقع | أبرز الميزات | الفجوات في نظامنا |
|----------|--------|--------------|-------------------|
| **Zenoti** | متعدد الفروع | منصة متكاملة: حجز + POS + CRM + تسويق + مخزون + جدولة + رواتب، **العضويات (Memberships)**، تكامل ذكي | عضويات، تسويق |
| **Fresha** | عالمي | حجز أونلاين، **قوالب صيغ الألوان (color formula)**، **حجز مزدوج مع فجوة معالجة**، تذكير تلقائي، **عضويات وتسويق مجاني** | عضويات |
| **Booksy** | عالمي | سوق مستهلكين، **رسوم إلغاء (cancellation fees)**، **دفع مسبق (prepayment)**، إدارة ورديات، جرد | رسوم إلغاء |
| **Vagaro** | عالمي | POS + **إكراميات (tips)** + عمولات + **بطاقات هدايا** + عضويات | إكراميات، بطاقات هدايا |
| **GlossGenius** | عالمي | حجز جميل، معالجة مدفوعات موحدة | تصميم البوابة |
| **Mindbody** | عالمي | عضويات، تكامل ClassPass، تسويق | عضويات |
| **Mangomint** | عالمي | بوتيك صغير، عضويات | عضويات |

### أهم الفجوات المشتركة (الأولوية المقترحة)

1. **العضويات/الباقات (Memberships)** — الاشتراكات الشهرية ترفع الإيرادات المتكررة (الصالونات ذات العضويات نمت 4× أسرع).
2. **بطاقات الهدايا (Gift Cards)** — بيع/استرداد/رصيد.
3. **الإكراميات (Tips)** — إضافة إكرامية للفاتورة وتوزيعها على الموظفة.
4. **رسوم الإلغاء والدفع المسبق** — حماية الإيرادات من عدم الحضور (no-shows).
5. **تذكير تلقائي (WhatsApp/SMS) بالمواعيد القادمة** — جاهز جزئيًا (Cloud API)، يُفعَّل تلقائيًا.
6. **حملات تسويقية** — إرسال رسائل لعملاء مختارين (عيد ميلاد، عروض، عملاء لم يزوروا منذ مدة).
7. **تحليلات لوحة التحكم** — رسوم بيانية (اتجاه المبيعات، أفضل الخدمات، تنبيه المخزون، المواعيد القادمة).
8. **طباعة الفواتير** — قالب طباعة متقن.
9. **سجل زيارات العميلة + إجمالي الزيارات وآخر زيارة** — إثراء ملف العميلة.
10. **تقويم هجري** في التقارير والمواعيد.

---

## 2. التكنولوجيا المقترحة (Tech Stack)

| المكوّن | الاختيار | السبب |
|---------|----------|-------|
| الواجهة الأمامية | React + Vite + TypeScript | واجهة سريعة وقابلة للتوسع |
| مكتبة UI | MUI (Material UI) | مكونات جاهزة تدعم RTL |
| إدارة الحالة | React Query + Zustand | بيانات الخادم والحالة المحلية |
| التوجيه | React Router | صفحات متعددة |
| الباكند | Node.js + Express / NestJS | Node.js مطلوب |
| قاعدة البيانات | PostgreSQL | قوية وموثوقة |
| ORM | Prisma | نمذجة و Migrations سهلة |
| المصادقة | JWT + bcrypt | آمنة |
| التحقق من المدخلات | Zod | تحقق قوي من النماذج |
| التقارير والتصدير | ExcelJS / PDFKit | تصدير Excel و PDF |
| اللغة | react-i18next (فرونت) + i18n على الباكند | ثنائية اللغة |
| الاختبار | Jest / Vitest + Supertest | جودة الكود |
| النسخ الاحتياطي | pg_dump + مهام مجدولة (node-cron) | نسخ قاعدة البيانات |

---

## 3. بنية المشروع (Monorepo)

```
saloon/
├── client/                        # الواجهة الأمامية React
│   ├── src/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   ├── i18n/                  # ملفات الترجمة (en.json, ar.json)
│   │   ├── theme/                 # تنسيقات MUI + RTL
│   │   ├── api/                   # استدعاءات API (axios) لكل وحدة
│   │   ├── stores/                # Zustand
│   │   ├── components/            # مكونات مشتركة (AppLayout, ShiftStatusBar ...)
│   │   ├── pages/
│   │   │   ├── public/            # بوابة العميلات (Landing, Booking, BookingStatus)
│   │   │   ├── LoginPage.tsx
│   │   │   ├── DashboardPage.tsx
│   │   │   ├── ClientsPage.tsx
│   │   │   ├── AppointmentsPage.tsx
│   │   │   ├── ServicesPage.tsx
│   │   │   ├── EmployeesPage.tsx
│   │   │   ├── AttendancePage.tsx
│   │   │   ├── InventoryPage.tsx
│   │   │   ├── AccountingPage.tsx
│   │   │   ├── OffersPage.tsx
│   │   │   ├── UsersPage.tsx
│   │   │   ├── ReportsPage.tsx
│   │   │   └── SettingsPage.tsx
│   │   └── router/
│   └── package.json
├── server/                        # الباكند Node.js
│   ├── src/
│   │   ├── index.ts               # نقطة الدخول
│   │   ├── app.ts                 # إعداد Express + تسجيل الوحدات
│   │   ├── config/
│   │   │   ├── env.ts             # متغيرات البيئة
│   │   │   └── database.ts        # اتصال Prisma
│   │   ├── middleware/
│   │   │   ├── auth.ts            # التحقق من JWT والصلاحيات
│   │   │   ├── errorHandler.ts
│   │   │   └── i18n.ts            # لغة الاستجابة
│   │   ├── modules/
│   │   │   ├── auth/
│   │   │   ├── public/            # بوابة العميلات
│   │   │   ├── clients/
│   │   │   ├── appointments/
│   │   │   ├── services/
│   │   │   ├── employees/
│   │   │   ├── attendance/
│   │   │   ├── shifts/
│   │   │   ├── inventory/
│   │   │   ├── suppliers/
│   │   │   ├── purchases/
│   │   │   ├── accounting/
│   │   │   ├── offers/
│   │   │   ├── loyalty/
│   │   │   ├── reports/
│   │   │   ├── users/
│   │   │   ├── notifications/
│   │   │   ├── settings/
│   │   │   └── backup/
│   │   │   └── (كل وحدة: controller + service + routes + validation)
│   │   ├── utils/
│   │   └── types/
│   ├── prisma/
│   │   ├── schema.prisma         # تعريف جداول PostgreSQL
│   │   └── migrations/
│   └── package.json
├── docker-compose.yml             # تشغيل PostgreSQL بسهولة
├── .env.example
├── README.md
└── docs/                          # التوثيق (README, features, user-manual, api, architecture)
```

---

## 4. قاعدة البيانات PostgreSQL (Schema عبر Prisma)

### 4.1 المستخدمون `users`
- id، username، passwordHash، role (ADMIN/RECEPTIONIST/STYLIST)، employeeId، isActive، **permissions[]**، createdAt

### 4.2 العملاء `clients`
- id، name، phone، whatsapp، email، birthdate، notes، totalSpent، **loyaltyPoints**، createdAt

### 4.3 الخدمات `services`
- id، nameAr، nameEn، category، price، durationMinutes، cost، isActive

### 4.4 الموظفات `employees`
- id، nameAr، nameEn، phone، role (STYLIST/BEAUTICIAN/RECEPTIONIST)، commissionRate، hireDate، isActive

### 4.5 المواعيد `appointments`
- id، clientId (FK)، employeeId (FK)، serviceId (FK)، date، startTime، endTime، status (BOOKED/ARRIVED/DONE/CANCELLED)، notes، createdAt

### 4.6 الفواتير `invoices`
- id، invoiceNo، clientId (FK)، employeeId (FK)، date، subtotal، discount، tax، total، paymentMethod (CASH/CARD/WALLET)، status، **offerCode**، **pointsEarned**، **pointsRedeemed**

### 4.7 تفاصيل الفواتير `invoice_items`
- id، invoiceId (FK)، serviceId/productId، description، quantity، unitPrice، lineTotal

### 4.8 المصروفات `expenses`
- id، date، category، amount، description، createdBy، createdAt

### 4.9 المنتجات `products`
- id، nameAr، nameEn، barcode، category، quantity، unit، costPrice، salePrice، minStock، supplier، **supplierId (FK)**

### 4.10 حركة المخزون `stock_movements`
- id، productId (FK)، type (IN/SALE/USAGE/LOSS)، quantity، date، referenceId

### 4.11 جداول الترجمة والبيانات العامة
- يمكن استخدام عمودين nameAr/nameEn لكل جدول يحتاج ترجمة (Services, Products, Employees).

### 4.12 حركات الولاء `loyalty_transactions`
- id، clientId (FK)، points، type (EARN/REDEEM)، balanceAfter، referenceId، note، createdAt

### 4.13 العروض `offers`
- id، code (فريد)، nameAr، nameEn، discountType (PERCENT/FIXED)، value، validFrom، validTo، minTotal، isActive

### 4.14 الحضور `attendance`
- id، employeeId (FK)، date، checkIn، checkOut، hoursWorked، notes — (فريد على employeeId+date)

### 4.15 الموردون `suppliers`
- id، name، phone، email، address، notes، isActive، createdAt

### 4.16 أوامر الشراء `purchase_orders`
- id، orderNo (فريد)، supplierId (FK)، date، status (PENDING/RECEIVED/CANCELLED)، subtotal، discount، total، paymentMethod، createdBy (FK)، notes

### 4.17 بنود أوامر الشراء `purchase_order_items`
- id، purchaseOrderId (FK)، productId (FK)، productName، quantity، unitCost، lineTotal

### 4.18 الإعدادات `settings`
- key (مفتاح أساسي)، value — مفاتيح معروفة: VAT_RATE، LOYALTY_POINTS_PER_CURRENCY، LOYALTY_POINT_VALUE، SALON_NAME_AR، SALON_NAME_EN

### 4.19 الإشعارات `notifications`
- id، type، target، message، status (PENDING/SENT/FAILED)، referenceId، createdAt

---

## 5. المصادقة والأدوار

| الدور | الصلاحيات |
|-------|-----------|
| ADMIN | كل شيء: التقارير، الأسعار، المحاسبة، إدارة الموظفات |
| RECEPTIONIST | المواعيد، العملاء، الفواتير، المدفوعات |
| STYLIST | عرض مواعيدها فقط |

- JWT (Access + Refresh tokens) عبر `jsonwebtoken`.
- كلمات مرور مشفرة عبر `bcrypt`.
- Middleware يتحقق من الدور لكل مسار.

---

## 6. الثنائية اللغة (i18n)

- `react-i18next` في الواجهة مع ملفي ترجمة `ar.json` و `en.json`.
- تبديل اللغة فوري (RTL عربي / LTR إنجليزي عبر توجيه MUI).
- تخزين اللغة المفضلة في localStorage.
- أسماء الخدمات والمنتجات تُخزّن بحقول مزدوجة (nameAr / nameEn) وتُعاد حسب لغة الطلب `?lang=en`.
- لغة الخادم تُحدد عبر هيدر `Accept-Language` أو معامل `lang`.

---

## 7. وحدات النظام بالتفصيل

### 7.1 المصادقة `auth`
- تسجيل دخول، تحديث التوكن، تسجيل الخروج.
- حماية المسارات بالصلاحيات.

### 7.2 لوحة التحكم `dashboard`
- ملخص اليوم: مواعيد اليوم، إيرادات، مصروفات، موظفات مشغولات.

### 7.3 العملاء `clients`
- CRUD + بحث (اسم/هاتف/واتساب).
- ملف العميل: سجل الزيارات، إجمالي الإنفاق، الخدمات المفضلة.

### 7.4 المواعيد `appointments`
- تقويم أسبوعي/يومي (MUI X Date Pickers).
- إنشاء/تعديل/إلغاء + كشف تعارض أوقات الموظفات.
- تغيير الحالة: BOOKED ← ARRIVED ← DONE / CANCELLED.
- إنشاء فاتورة من موعد منجز بضغطة واحدة.

### 7.5 الخدمات `services`
- CRUD بأسماء مزدوجة اللغة + أسعار ومدة وتصنيف.

### 7.6 الموظفات `employees`
- CRUD + نسب العمولة + جدول العمل + تقرير الإنتاجية.

### 7.7 المخزون `inventory`
- المنتجات والكميات والحد الأدنى للتنبيه.
- حركة المخزون (استلام/بيع/استهلاك/تلف).

### 7.8 المحاسبة `accounting`
- الفواتير والمدفوعات (نقدي/كارت/محفظة).
- خصم عمولة الموظفة تلقائياً.
- المصروفات حسب التصنيف.
- تقرير الأرباح والإغلاق اليومي.

### 7.9 التقارير `reports`
- مبيعات يومية/أسبوعية/شهرية.
- أكثر الخدمات مبيعاً، أكثر العملاء إنفاقاً.
- إنتاجية الموظفات وعمولاتهن.
- تصدير Excel / PDF.

### 7.10 نقطة البيع `POS`
- إنشاء فاتورة من خدمات ومنتجات (بنود متعددة).
- خصومات، ضريبة VAT، طرق دفع (نقدي/كارت/محفظة).
- ربط الفاتورة بوردية مفتوحة + حساب عمولة الموظفة.
- تطبيق كود عرض + اكتساب/استرداد نقاط الولاء.

### 7.11 نقاط الولاء `loyalty`
- رصيد نقاط لكل عميلة + سجل حركات (اكتساب/استرداد).
- تعديل يدوي للمدير، واكتساب تلقائي من الفواتير وفق الإعدادات.

### 7.12 العروض `offers`
- كوبونات خصم (نسبة/مبلغ ثابت) + صلاحية وحد أدنى وتفعيل.
- تحقق من صحة الكود عند الفوترة.

### 7.13 الحضور `attendance`
- تسجيل حضور/انصراف يومي + ساعات العمل + ملخص حسب الفترة.

### 7.14 المستخدمون والصلاحيات `users`
- إدارة حسابات المستخدمين + صلاحيات تفصيلية (permissions).
- ربط المستخدم بموظفة (اختياري) وتعطيل/تفعيل الحساب.

### 7.15 الموردون والمشتريات `suppliers` / `purchases`
- إدارة الموردين + أوامر الشراء (إنشاء/استلام/إلغاء).
- استلام الأمر يرفع مخزون المنتجات تلقائياً.

### 7.16 الإشعارات `notifications`
- جدول إشعارات (نوع/هدف/رسالة/حالة).
- إرسال WhatsApp عبر Cloud API (تذكير المواعيد).

### 7.17 الإعدادات `settings`
- قراءة/تعديل الإعدادات العامة والولاء (المدير فقط للكتابة).
- مفاتيح معروفة مع قيم افتراضية وتسميات ثنائية اللغة.

### 7.18 النسخ الاحتياطي `backup`
- تصدير JSON كامل + تفريغ pg_dump (SQL) + تصدير CSV للجداول الرئيسية.
- المدير فقط، مع أسماء ملفات بوقت التصدير.

---

## 8. مراحل التنفيذ (Roadmap)

| المرحلة | المدة | المحتوى | الحالة |
|---------|-------|---------|--------|
| 0 - التأسيس | أسبوع 1 | تهيئة Monorepo، Docker + PostgreSQL، Prisma schema و migrations | ✅ منجزة |
| 1 - المصادقة والأدوار | أسبوع 2 | JWT، تسجيل الدخول، Middleware الصلاحيات، شاشة Login | ✅ منجزة |
| 2 - الخدمات والعملاء | أسبوع 3-4 | CRUD للخدمات والعملاء + البحث + إعداد i18n | ✅ منجزة |
| 3 - المواعيد | أسبوع 5-6 | التقويم الأسبوعي + إنشاء/تعديل/إلغاء + كشف التعارض | ✅ منجزة |
| 4 - الفوترة والموظفون | أسبوع 7-8 | الفواتير، العمولات، إدارة الموظفات | ✅ منجزة |
| 5 - المخزون والمحاسبة | أسبوع 9-10 | المخزون، المصروفات، الإغلاق اليومي | ✅ منجزة |
| 6 - التقارير | أسبوع 11 | التقارير والتصدير Excel/PDF | ✅ منجزة |
| 7 - الاختبار والنشر | أسبوع 12 | **الاختبار الآلي:** ✅ Vitest — 7 ملفات اختبار / 57 اختبارًا. **Docker Compose:** ✅ postgres + app + web. **نشر إنتاج Railway:** ✅ `https://mira-production-296a.up.railway.app` (GitHub: `mozn1986/Mira`, `h-abar/Mira`) | ✅ منجزة |
| 8 - التوسعة (الميزات الجديدة) | أسبوع 13-14 | نقطة البيع، نقاط الولاء، العروض، الحضور، المستخدمون والصلاحيات، الموردون والمشتريات، الإشعارات، الإعدادات، النسخ الاحتياطي، بوابة العميلات | ✅ منجزة (النسخ الاحتياطي SQL يعتمد على توفر pg_dump) |
| 8.1 - دورة تحسين الجودة والمراجعة الشاملة | — | مراجعة كاملة لـ schema وكل الوحدات + إصلاحات: (1) أتمتة الولاء (اكتساب/استرداد) داخل معاملة الفاتورة لحساب `pointsEarned` الصحيح ومنع التباين، (2) تثبيت الإجمالي عند الصفر عند تطبيق خصومات كبيرة، (3) فحص `isActive` فوري في `requirePermission`، (4) سد ثغرات POS (كود كوبون + استرداد نقاط + عرض النقاط المكتسبة في نافذة النجاح) | ✅ منجزة |
| 8.2 - فرض الأذونات على جميع الوحدات | — | استبدال `requireRole` بـ `requirePermission` في كل المسارات (عملاء، مواعيد، خدمات، موظفات، مخزون، محاسبة، تقارير، ورديات، موردون، مشتريات، إعدادات، إشعارات، ولاء) + تصفية كل عناصر القائمة والأزرار في الواجهة حسب الصلاحيات + تعطيل استعلامات لوحة التحكم بدون صلاحية | ✅ منجزة |
| 8.3 - تطبيق | — | إضافة مفاتيح `shifts.read`, `shifts.write`, `notifications` لنظام الصلاحيات | ✅ منجزة |
| 9 - إنجاز المستقبلي القابل للتنفيذ | — | **جدولة pg_dump تلقائية عبر node-cron** (يوميًا 2 صباحًا، الاحتفاظ 30 يومًا، دليل النسخ `./backups`، JSON + SQL، تنظيف تلقائي، نقطتا `/backup/trigger` و`/backup/schedule` + بطاقة حالة في صفحة الإعدادات). **تذكير WhatsApp عبر node-cron** (`REMINDER_CRON`، `REMINDER_HOURS_BEFORE`، نقطة حالة الجدولة). **صندوق الإشعارات الداخلي** (صفحة `/admin/notifications` مع فلاتر الحالة/النوع، إعادة إرسال للفاشلة، بطاقة حالة جدولة التذكيرات). **تبويب البيع السريع للكافتيريا في POS** (تصنيف `cafeteria`). **ZATCA المرحلة الأولى: رمز QR للفاتورة (بيع)** (TLV base64 عبر `zatcaQR`) في الفواتير المطبوعة مع خانة إعدادات لاحقًا | ✅ منجزة |
| 9.1 - إنجاز المستقبلي (2) | — | **وحدة ZATCA**: توليد شهادة امتثال ذاتية (sandbox/self-signed)، CSR، XML بصيغة UBL 2.1 موقّعة (SHA-256 + ECDSA P-256)، رمز QR (TLV base64)، إعدادات عبر `ZATCA_*`، نقاط `/api/zatca/{setup,csr,status,invoices/:id/xml,invoices/:id/qr,test}` (إنتاجية تحتاج شهادة من ZATCA). **الفروع المتعددة**: نموذج `Branch` + `branchId` على المنتجات/الفواتير/الورديات/المستخدمين + CRUD `/api/branches` (بصلاحية `branches`) + صفحة الفروع في الواجهة + منتقي فرع في الشريط العلوي + تصفية المخزون/المحاسبة/نقطة البيع بالفرع. **المدفوعات الإلكترونية**: نموذج `Payment` + CRUD `/api/payments` (بصلاحية `payments`/`accounting.read`) + بوابة محاكاة (`transactionId` من نوع `SIMULATED-…`) + طريقة دفع `ELECTRONIC` في POS والمحاسبة مع إنشاء دفعة تلقائي. **تفعيل واتساب من الإعدادات**: مفاتيح `WHATSAPP_*` + اختبار `POST /notifications/test`. **تطبيق ويب للجوال PWA**: `manifest.webmanifest` + أيقونات + `sw.js` (الوضع المستقل وتثبيت) | ✅ منجزة |
| 9.2 - بقايا المستقبلي | — | ربط بوابة دفع حقيقية فعليًا (بدل المحاكاة) ⏳ · توكن واتساب إنتاجي حقيقي ⏳ · شهادة ZATCA صادرة رسميًا ⏳ · ~~تعميق التقارير/الإغلاق اليومي حسب الفرع~~ ✅ منجز (جميع نقاط `/api/reports/*` والتصدير وتحليلات لوحة التحكم تقبل معامل `branchId` اختياريًا) | ⏳ جزئي — بند تقارير الفرع منجز، والباقي مستقبلي |
| 10 - ميزات المنافسين ذات الأولوية | أسبوع 15-16 | **العضويات/الباقات** (اشتراكات شهرية وربطها بالفواتير)، **بطاقات الهدايا** (رصيد/استرداد)، **الإكراميات** على الفواتير، **رسوم الإلغاء/الدفع المسبق**، **حملات تسويقية** (عيد ميلاد/عروض/إعادة استقطاب)، **تحليلات لوحة التحكم** (رسوم بيانية + تنبيهات)، **طباعة الفواتير**، **سجل زيارات وإحصائيات العميلة**، **تقويم هجري** | ✅ منجزة — **الواجهة الخلفية**: نموذجا `MembershipPlan`/`ClientMembership`/`GiftCard` في Prisma، وحدتا `memberships` و`giftcards`، حقول `tip`/`giftCardCode` في المحاسبة، `cancellationFee` في المواعيد، `sendCampaign` في الإشعارات، `dashboard-analytics` في التقارير، إحصائيات زيارة/خدمة مفضلة في العملاء. **الواجهة الأمامية**: صفحات العضويات/بطاقات الهدايا/الحملات مع صلاحياتها، لوحة تحكم بتحليلات أسبوعية ورسوم بيانية، حقول إكرامية + بطاقة هدايا في POS والمحاسبة، طباعة فاتورة، سجل زيارات، رسوم إلغاء عند إلغاء موعد، وتقويم هجري (Umm al-Qura) |
| 11 - تحسينات شاملة وهوية | أسبوع 17 | **إعادة تسمية النظام**: اسم العلامة التجارية **«ميرا — Mira»**. **تحسينات UX**: أزرار سريعة في لوحة التحكم، طباعة إيصال POS، حوارات MUI للتأكيد، منع تجاوز المخزون | ✅ منجزة |
| 12 - تشغيل إنتاج وتحسينات سبتمبر 2026 | — | **إعدادات:** حفظ مستقل لكل قسم. **POS:** ترتيب (عميلة → دفع → خصومات/ولاء) — الخصم/الكوبون/بطاقة الهدايا/النقاط بعد اختيار عميلة. **العضويات:** ربط باقات↔خدمات، تعيين ببحث عميلات. **بوابة عامة:** حجز حسب دوام الصالون، بحث حالة الحجز، أيقونات تواصل. **تصدير:** تجميع Excel/PDF + إصلاح PDF. **توثيق:** `docs/user-manual.md` + `docs/بيانات-الدخول.xlsx` + تحديث خطة العمل | ✅ منجزة |

---

## 9. المتطلبات الوظيفية (MVP)

1. تسجيل دخول بالأدوار (Admin / Receptionist / Stylist).
2. إدارة الخدمات والأسعار (ثنائية اللغة).
3. إدارة العملاء والبحث.
4. إنشاء وتتبع المواعيد مع كشف التعارض.
5. إصدار فاتورة من موعد منجز + حساب العمولة.
6. تسجيل المصروفات وتقرير ملخص اليوم.
7. تبديل اللغة عربي/إنجليزي.

---

## 10. متطلبات التشغيل

- Docker + Docker Compose (PostgreSQL + api + web).
- أو تشغيل منفصل: Node 20+، PostgreSQL 16+.
- متغيرات البيئة في `.env`: `DATABASE_URL`، `JWT_SECRET`، `PORT`، `CORS_ORIGIN`.

---

## 11. ملاحظات مستقبلية

- إشعارات WhatsApp/SMS لتذكير المواعيد (جاهزة عبر Cloud API، يُنصح بربط خط إنتاج وتوثيق فعلي).
- لوحة إشعارات داخلية (المواعيد القادمة).
- طباعة الفواتير.
- تطبيق موبايل (React Native) يستهلك نفس الـ API.
- نسخ احتياطي تلقائي عبر pg_dump مع node-cron (جدولة دورية + تنظيف النسخ القديمة).
- الفوترة الإلكترونية ZATCA (التكامل مع مزود معتمد وإصدار فواتير متوافقة).
- نقاط بيع متعددة (فروع/مستودعات متعددة).
- نقطة بيع كافتيريا مستقلة.
- تعزيز صلاحيات المستخدمين بمستويات أكثر تفصيلاً لكل شاشة.